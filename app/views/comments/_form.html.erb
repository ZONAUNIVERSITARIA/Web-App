<%= form_for ([@article, @article.comments.build]) do |f| %>
	<div><%= f.text_area :content, placeholder: "Escribe aquí tu comentario" %></div>
	<div><%= f.submit "Comentar", class: "button expanded button" %></div>
<%end%>