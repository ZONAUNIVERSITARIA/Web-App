<%= form_for ([@article, @article.comments.build]) do |f| %>
	<div><%= f.text_area :content, placeholder: "Escribe aquí tu comentario", style:"height: 200px;" %></div>
	<div><%= f.submit "Comentar", class: "btn be-blue expanded button" %></div>
<%end%>